import React from "react";

export const Button = ({ children, ...props }) => {
  return (
    <button
      style={{
        padding: 12,
        borderRadius: 8,
        fontWeight: "bold",
      }}
      {...props}
    >
      {children}
    </button>
  );
};
