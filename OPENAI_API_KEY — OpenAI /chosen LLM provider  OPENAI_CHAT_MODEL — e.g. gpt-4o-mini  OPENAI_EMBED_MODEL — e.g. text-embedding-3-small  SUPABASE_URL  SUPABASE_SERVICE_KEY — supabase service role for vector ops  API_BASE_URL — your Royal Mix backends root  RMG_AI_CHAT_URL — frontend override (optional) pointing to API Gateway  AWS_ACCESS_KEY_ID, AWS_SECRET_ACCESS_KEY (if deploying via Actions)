# from repo root
cd services/ai
npm install
# create a small test script
node -e 'require("./assistant/chatHandler").handler({ body: JSON.stringify({ userId:"u1", message:"Find me Royal Chicken near me" }) }).then(r=>console.log(r))'
