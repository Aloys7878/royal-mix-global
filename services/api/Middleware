export const apiKeyGuard = (req: any, res: any, next: any) => {
  const apiKey = req.headers["x-api-key"];

  if (!apiKey || apiKey !== process.env.ROYAL_API_KEY) {
    return res.status(401).json({ success: false, message: "Invalid API Key" });
  }

  next();
};
